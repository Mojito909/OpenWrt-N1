#
# Copyright (c) 2019-2020 P3TERX <https://p3terx.com>
#
# This is free software, licensed under the MIT License.
# See /LICENSE for more information.
#
# https://github.com/P3TERX/Actions-OpenWrt
# Description: Build OpenWrt using GitHub Actions
#
# 优化调整 by https://github.com/quanjindeng/Actions_OpenWrt-Amlogic
#

name: 编译固件（LEDE）

on:
  repository_dispatch:
  workflow_dispatch:

  schedule:
    - cron: 0 16 * * 4


env:
  REPO_URL: https://github.com/coolsnowwolf/lede
  REPO_BRANCH: master
  FEEDS_CONF: LEDE/feeds.conf.default
  CONFIG_FILE: LEDE/.config-lede
  DIY_P1_SH: diy-part1.sh
  DIY_P2_SH: LEDE/diy-part2.sh
  TZ: Asia/Shanghai
  DATE: ${{ github.run_number }}


jobs:
  build:
    runs-on: ubuntu-22.04
    env:
      WHOAMI: ${{ github.actor }}
      PACKAGE_SOC: s905d


    steps:
    - name: 创建编译空间挂载点
      run: |
        sudo mkdir -p /workdir
        sudo chown $USER:$USER /workdir


    - name: 最大化编译空间
      uses: easimon/maximize-build-space@master
      with:
        root-reserve-mb: 4096
        swap-size-mb: 1024
        temp-reserve-mb: 200
        build-mount-path: /workdir
        overprovision-lvm: 'true'
        remove-dotnet: 'true'
        remove-android: 'true'
        remove-haskell: 'true'
        remove-codeql: 'true'
        remove-docker-images: 'true'

  
    - name: 检出代码
      uses: actions/checkout@v4


    - name: 检查服务器配置
      run: |
        echo "警告⚠"
        echo "分配的服务器性能有限，若选择的插件过多，务必注意CPU性能！"
        echo "云编译建议取消勾选Node.js及其相关插件！"
        echo "已知CPU型号（降序）：8370C,8272CL,8171M,E5系列"
        echo "--------------------------CPU信息--------------------------"
        echo "CPU物理数量:$(cat /proc/cpuinfo| grep "physical id"| sort| uniq| wc -l)"
        echo -e "CPU核心及版本信息：$(cat /proc/cpuinfo | grep name | cut -f2 -d: | uniq -c) \n"
        echo "--------------------------内存信息--------------------------"
        echo "已安装内存详细信息："
        sudo lshw -short -C memory | grep GiB
        echo -e "\n"
        echo "--------------------------硬盘信息--------------------------"
        echo -e  "硬盘数量：$(ls /dev/sd* | grep -v [1-9] | wc -l) \n"
        echo "硬盘详情："
        df -Th


    - name: 初始化编译环境
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo apt-get update -y
        sudo apt-get full-upgrade -y
        sudo apt-get install -y ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
        bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib \
        git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libglib2.0-dev libgmp3-dev libltdl-dev \
        libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libreadline-dev libssl-dev libtool lrzsz \
        mkisofs msmtp nano ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pyelftools \
        libpython3-dev qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip \
        vim wget xmlto xxd zlib1g-dev
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        sudo timedatectl set-timezone "$TZ"
        echo "硬盘详情："
        df -Th
  

    - name: 克隆源码
      working-directory: /workdir
      run: |
        git clone $REPO_URL -b $REPO_BRANCH openwrt
        ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt


    - name: 加载自定义feeds
      working-directory: /workdir/openwrt
      run: |
        [ -f "$GITHUB_WORKSPACE/LEDE/feeds.conf.default" ] && \
          cp "$GITHUB_WORKSPACE/LEDE/feeds.conf.default" feeds.conf.default
        chmod +x "$GITHUB_WORKSPACE/diy-part1.sh"
        "$GITHUB_WORKSPACE/diy-part1.sh"


    - name: 更新feeds
      run: cd /workdir/openwrt && ./scripts/feeds update -a

    
    - name: 安装feeds
      run: cd /workdir/openwrt && ./scripts/feeds install -a


    - name: 加载自定义配置
      run: |
        [ -d "$GITHUB_WORKSPACE/files" ] && cp -r "$GITHUB_WORKSPACE/files" /workdir/openwrt/
        [ -f "$GITHUB_WORKSPACE/LEDE/.config-lede" ] && cp "$GITHUB_WORKSPACE/LEDE/.config-lede" /workdir/openwrt/.config
        chmod +x "$GITHUB_WORKSPACE/LEDE/diy-part2.sh"
        cd /workdir/openwrt
        "$GITHUB_WORKSPACE/LEDE/diy-part2.sh"
        ./scripts/feeds update -a
        ./scripts/feeds install -a


    - name: 下载软件包
      run: |
        cd /workdir/openwrt
        make defconfig
        grep -E '^CONFIG_PACKAGE_(luci-app-adguardhome|adguardhome|luci-app-alist|alist|luci-app-passwall|xray-core|luci-app-ttyd|ttyd|luci-i18n-ttyd-zh-cn)=' .config || true
        ./scripts/feeds search luci-app-passwall || true
        echo "===== 编译目标校验 (.config) ====="
        grep -E '^CONFIG_TARGET|^CONFIG_LINUX_' .config | head -n 50 || true
        grep -Eq '^CONFIG_TARGET_(armvirt|armsr)=y' .config || { echo "ERROR: .config 未生效或目标错误（期望 ARM 目标：armvirt/armsr），请检查配置加载步骤。"; exit 1; }
        make download -j8
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;
        echo "硬盘详情："
        df -Th


    - name: 编译固件
      run: |
        cd /workdir/openwrt
        echo -e "$(nproc) thread compile"
        make -j$(nproc) || { make -j1 V=sc 2>&1 | tee build.log; exit ${PIPESTATUS[0]}; }
        echo "status=success" >> $GITHUB_OUTPUT
        grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME
        [ -s DEVICE_NAME ] && echo "DEVICE_NAME=_$(cat DEVICE_NAME)" >> $GITHUB_ENV
        echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV

    - name: 输出失败日志
      if: failure()
      run: |
        cd /workdir/openwrt
        echo "===== .config (TARGET 摘要) ====="
        grep -E '^CONFIG_TARGET|^CONFIG_LINUX_' .config | head -n 200 || true
        echo "===== diffconfig ====="
        ./scripts/diffconfig.sh || true
        echo "===== tmp/log 列表 ====="
        ls -lah tmp/log 2>/dev/null || true
        latest_log="$(ls -1t tmp/log/* 2>/dev/null | head -n 1 || true)"
        if [ -n "$latest_log" ]; then
          echo "===== 最新 tmp/log: $latest_log (末尾400行) ====="
          tail -n 400 "$latest_log" || true
        fi
        if [ -f build.log ]; then
          echo "===== build.log (末尾400行) ====="
          tail -n 400 build.log || true
        fi


    - name: 清理磁盘空间
      if: (!cancelled())
      run: |
        echo "清理之前："
        df -hT
        mkdir openwrt-armvirt
        mkdir -p /workdir/upload
        # 使用绝对路径操作，避免软链接解析问题
        cp /workdir/openwrt/bin/targets/*/*/*.tar.gz /workdir/upload/
        mv /workdir/openwrt/bin/targets/*/*/*.tar.gz openwrt-armvirt/
        echo -e "\n"
        echo "列出空间占用前10的目录或文件："
        sudo du -h --max-depth=1 /workdir | sort -nr | head -10
        rm -rf /workdir/openwrt/ && sync
        echo -e "\n"
        echo "清理之后："
        df -hT
        echo -e "\n"
        echo "/workdir路径文件"
        ls -l /workdir/
        echo -e "\n"
        echo "列出空间占用前10的目录或文件："
        sudo du -h --max-depth=1 /workdir | sort -nr | head -10


    - name: 打包内核
      uses: ophub/flippy-openwrt-actions@main
      env:
        WHOAMI: ${{ env.WHOAMI }}
        PACKAGE_SOC: ${{ env.PACKAGE_SOC }}
        OPENWRT_ARMVIRT: openwrt-armvirt/*.tar.gz
        KERNEL_VERSION_NAME: 5.15.105_6.6.1
       # KERNEL_AUTO_LATEST: false
       # KERNEL_REPO_URL: https://github.com/quanjindeng/Actions_OpenWrt-Amlogic/tree/main/kernel
        

    - name: 整理文件
      run: |
        mkdir -p /workdir/upload
        src_path="/opt/openwrt_packit/output"
        if [ ! -d "$src_path" ] || [ -z "$(ls -A "$src_path" 2>/dev/null)" ]; then
          ls -lR /opt/openwrt_packit/ || true
          exit 1
        fi
        cp -r "$src_path"/* /workdir/upload/

    
    - name: 发布固件
      uses: ncipollo/release-action@v1
      with:
        tag: openwrt_armvirt_v8_${{ env.FILE_DATE }}
        artifacts: /workdir/upload/*
        allowUpdates: true
        token: ${{ secrets.GITHUB_TOKEN }}
        body: |
          # 🚀 OpenWrt固件发布 - N1盒子专用
          
          ## 📋 固件信息
          - **默认IP地址**: `192.168.1.10`
          - **默认用户名**: `root`
          - **默认密码**: `password`
          - **适用平台**: Amlogic S905D (N1盒子)
          
          ## 🛠️ 安装与更新
          **U盘安装**: 写入U盘启动 → 执行 `openwrt-install-amlogic` → 按提示完成eMMC写入
          
          **Web界面**: 登录Luci → 使用"晶晨宝盒"插件 → 一键安装/更新
          
          **固件更新**: 将固件放入 `/mnt/mmcblk2p4/` → 执行 `openwrt-update-amlogic`
          
          ## ✨ 功能特色
          ✅ 集成常用插件 | ✅ 一键eMMC安装 | ✅ 在线更新 | ✅ N1性能优化
          
          ## ⚠️ 重要提示
          - 固件文件名：`openwrt_s905d_n1_XX_XX`
          - 确保N1盒子型号匹配，建议首次使用前备份原系统


    - name: 清理Actions空间
      uses: Mattraks/delete-workflow-runs@main
      with:
          token: ${{ secrets.GITHUB_TOKEN }}
          仓库: ${{ github.repository }}
          keep_minimum_runs: 0
          retain_days: 7
          #retain_days: ${{ github.event.inputs.action_days }}
    
          
    - name: 删除多余releases
      uses: dev-drprasad/delete-older-releases@master
      with:
        repo: ${{ github.repository }}
        keep_latest: 6
        #keep_latest: ${{ github.event.inputs.rele }}
        delete_tags: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
